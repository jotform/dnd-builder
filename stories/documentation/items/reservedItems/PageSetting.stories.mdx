
import { useState } from 'react';
import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { BuilderProvider } from '../../../../src/utils/builderContext.js';
import { PropProvider } from '../../../../src/utils/propContext.js';
import ReportWrapper from '../../../../src/components/ReportWrapper';
import RightPanel from '../../../../src/components/Panels/RightPanel';
import pages from '../../../config/examplePages.js';

<Meta title="Documentation/Items/Reserved Items/Page Setting" />

# Page Item

This item is reserved for the general layout settings.

its item type is  `__page__`. and you shoul not use this keyword for other items.
examplePages

<Canvas>
    <BuilderProvider>
      {(() => {
        const [_layout, editLayout] = useState(pages);
        return (
          <div  style={{ display: 'block', height: '350px' }}>
            <ReportWrapper>
              <PropProvider settings={_layout}>
                <BuilderProvider isRightPanelOpen editedElement="p_72m4j4">
                  <RightPanel
                    pages={pages}
                    onSettingChange={(id, setting) => {editLayout({ ..._layout, ...setting})}}
                  />
                </BuilderProvider>
              </PropProvider>
            </ReportWrapper>
          </div>
        )
      })()}
    </BuilderProvider>
</Canvas>