import * as icons from '../utils/icons';

export const elementIcons = {
  chart: icons.column,
  default: icons.text,
  header: icons.header,
  image: icons.image,
  shapes: icons.shapes,
  text: icons.textElement,
};
